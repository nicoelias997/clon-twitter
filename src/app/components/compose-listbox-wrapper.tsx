import React from "react"

export const ListboxWrapper = ({ children }: { children: React.ReactNode }) => (
  <div className="w-full max-w-full border-small rounded border-none">
    {children}
  </div>
)
